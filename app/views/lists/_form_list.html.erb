<%= form_for(@list) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
		<div class="field">
			<%= f.text_field :title, placeholder: 'リスト名を入力してください', class: 'form-control' %>
		</div>
		<div class="submit">
			<%= f.submit "リストの作成", class: 'btn btn-primary' %>
		</div>
<% end %>
